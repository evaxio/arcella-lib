package aes

/*
aconst (
	MySecret1 = "... some ..."
)

func TestDES(t *testing.T) {

	keyStr := MySecret1[len(MySecret1)-8:]
	fmt.Println("keyStr: ", keyStr)
	key := []byte(keyStr)

	data := MySecret1[:len(MySecret1)-8]
	fmt.Println("data: ", data)

	dataBytes, _ := base64.StdEncoding.DecodeString(data)
	if result, err := DesDecrypt(dataBytes, key); err == nil {
		fmt.Println("result: ", string(result))
	} else {
		panic(err)
	}

}
*/
